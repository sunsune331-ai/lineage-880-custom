# CHAT ARCHIVE 2026-09-07 18:44 +08:00

archive_type: `AI_HANDOFF_DEDUP`
scope: `current long chat only; durable facts, evidence, failures, state, next actions`
precedence: `latest explicit user Goal > CODEX_STATUS > REVIEWER_FEEDBACK > this archive > older handoff`
build_scope: `32-bit Lineage 8.8 LinLogin.bin; runtime VA values belong to the recorded executable build; heap/PID values are session-local`
do_not_treat_as_transcript: `true`

## 1. Executive Summary

- Project combines an 8.8 private-server codebase with read-only client reverse engineering, inventory UI/reorder mapping, resource/GFX investigation, and a separately tracked 783-article J.J. research corpus.
- Current completed action is preservation of this chat. No Client/Server/Database/resource behavior is changed by this archive.
- Strongest inventory result: main inventory has statically mapped reorder/refresh candidates and a prepared one-shot capture plan, but the actual manual-drag entry and ABI are not yet runtime-confirmed.
- Strongest research state: formal ledger is `88 / 783`, remaining `695`; rereads do not increase the count. Future work is user-triggered, at most 10 articles per batch.
- Start a replacement task by reading this file, `AI_HANDOFF.md`, `CODEX_STATUS.md`, latest `REVIEWER_FEEDBACK.md`, then the relevant part of `CLIENT_UI_FUNCTION_MAP.md`.

## 2. Project Goals

- PG-01 CURRENT: preserve the useful state of this long chat in GitHub, with evidence levels and direct restart instructions.
- PG-02 LONG_TERM_CLIENT: map `RenewalInventoryUI / InvWindow → source container → order producer → reorder → refresh`, then derive the smallest safe PoC.
- PG-03 LONG_TERM_RESOURCE: support fast on-demand identification and validation of NPC/item GFX and SPR/PAK/IDX/BIN resources.
- PG-04 LONG_TERM_RESEARCH: retain all technically relevant Client/Server/Protocol/resource/tool/file-format evidence from the 783-article corpus; uncertainty is labeled, not discarded.
- PG-05 WORKFLOW: one user Goal should drive Executor work, status update, Reviewer check, correction/recheck, and GitHub checkpoint; maximum three review cycles.
- PG-06 USER_INTENT: do not repeat already disproven work, do not require manual message shuttling, and stop only for defined user/system/client-mutation boundaries.

## 3. Confirmed Facts

Evidence codes: `RUNTIME` direct live observation; `STATIC` disassembly/RTTI/vtable/xref; `SOURCE` repository source/schema; `TOOL` local tool/config execution; `POLICY` explicit user rule. Runtime addresses below are historical unless a new session rebind succeeds.

- CF-01 `[SOURCE]` Git remote is `https://github.com/sunsune331-ai/lineage-880-custom.git`; repository intentionally excludes client executables/resources, dumps, secrets, caches, and build output. Evidence: Git remote/README.
- CF-02 `[STATIC+RUNTIME]` main UI path is `RenewalInventoryUI / InvWindow`; static globals are `[0x01A960B4]` with grid at UI `+0x15C`, InvWindow at `+0x168`, and manager global `[0x01952748]`. Evidence: `CLIENT_UI_FUNCTION_MAP.md`.
- CF-03 `[RTTI+RUNTIME]` `PromoteDollUI != 主背包`; its recovered path is reusable shared-UI knowledge only. Evidence: RTTI/vtable/global-slot analysis in `CLIENT_UI_FUNCTION_MAP.md`.
- CF-04 `[RUNTIME, historical session]` main objects were UI `0x1E3ECBE0`, InventoryItemGrid `0x1E44A6C8`, InvWindow `0x1E3EB0C8`, manager `0x2D523AF8`. These values are not reusable across restarts.
- CF-05 `[RUNTIME]` InventoryItemGrid `+0x190/+0x194/+0x198` form `vector<InventoryItemIcon-like*>` begin/end/capacity; one historical buffer was `0x2CC72F48/0x2CC73054/0x2CC730C0` with 67 icons.
- CF-06 `[RUNTIME+STATIC]` manager `+0x54` is a source `vector<Item*>`; the observed fully loaded inventory also contained 67 entries. Evidence: before/after snapshots and rebuild call graph.
- CF-07 `[STATIC+RUNTIME]` three distinct order representations exist: manager source order (`manager+0x54`), persisted item-ID order (`UI+0x16C`), and grid layout-index order (`grid+0x1BC`). They must not be conflated.
- CF-08 `[STATIC]` main refresh/rebuild chain is `manager+0x54 → 0x00DF8450 → 0x00DF9E60 → 0x00DFA580 → 0x00FC2520`, with create-missing `0x00DFA020`, swap helper `0x00DF5480`, and finalizers `0x00DFAF60/0x00DFB260`.
- CF-09 `[STATIC]` UI reorder commit candidate is grid vslot `+0x1A0 = 0x00DFB6E0`, delegating through `0x00FC2BB0` to `0x00DFC6B0`; `0x00DFC6B0` clears UI `+0x16C`, snapshots current grid model order through `0x00DF85B0`, and appends item IDs.
- CF-10 `[STATIC]` source-range reorder is `0x00DE0E40` on manager `+0x54`; grid vslot `+0x1A4 = 0x00DFBFA0` calls it and then `0x00DFA580`; `0x00DF9EF0` is a single-position adjustment wrapper followed by refresh.
- CF-11 `[STATIC; runtime entry not proven]` `0x00DE1D40` is a bulk order-adjust candidate using manager `+0x54`, one of the 180-DWORD tables at manager `+0x68/+0x340`, a generated key array, and two-stage swaps via `0x00DD9FC0`.
- CF-12 `[RUNTIME+STATIC]` new-item purchase confirmed `0x00FBC6F0` constructs one InventoryItemIcon-like object and appends it to grid `+0x190`; call site is `0x00FBC7CC`. It is not a bulk rebuild/reorder entry.
- CF-13 `[RUNTIME+STATIC]` `0x00FC4A70` is the vector append helper used by CF-12; observed data writer is `0x00FC4BCE`. Full registers/stack from that hit were not preserved in docs.
- CF-14 `[RUNTIME]` icon `+0x94/+0x98` is a POINT/layout coordinate pair repeatedly rewritten by idle layout; actual store was `0x004C32B8` in helper `0x004C32B0`, while the data-break exception EIP appeared at `0x004C32BA`.
- CF-15 `[RUNTIME]` one attempted drag comparison left selected icons, grid `+0x190`, grid `+0x1BC`, and manager `+0x54` unchanged; it did not prove a reorder event.
- CF-16 `[STATIC]` named sort engine family `0x00DEF5F0/0x00DEF750/0x00DEEEB0` and predicate names exist, but exact refs to the mapped main inventory pipeline are zero; no main-inventory sort role is confirmed.
- CF-17 `[RTTI+STATIC]` PromoteDollUI source flow is `provider+0x1FC → 0x00BA35E0 → Vector28 at +0x1DC → 0x00BA1AF0 clear+loop → PromoteDollUI grid+0x170 → shared vslot+0x188 0x00FBC6F0`; wrapper/core are `0x00BA5800/0x00BA5820`.
- CF-18 `[VTABLE+STATIC]` `XmlScrollGridIcon` and `InventoryItemGrid` share grid implementations: clear vslot `+0x184 = 0x00FBD520`, create/add vslot `+0x188 = 0x00FBC6F0`, add-existing vslot `+0x18C = 0x00FBC670`.
- CF-19 `[RUNTIME methodology]` Windows data-break exception EIP is the instruction after the completed store; writer identification requires backward decode plus effective-address verification.
- CF-20 `[RUNTIME methodology]` PID, heap objects, singleton live values, and vector buffers are session-local and must never be reused after client restart.
- CF-21 `[STATIC methodology]` static VAs are accepted for the recorded build only and must pass byte/AOB rebind before a new live capture.
- CF-22 `[TOOL]` local `tools/inventory_reorder_capture.py selftest` passed with `status=PASS`, `anchors=5`, `failures=[]`; four live anchors plus `DE1D40` static supplement are represented.
- CF-23 `[RUNTIME state at 2026-09-07 18:44 +08:00]` `LinLogin.bin` was not running at the latest check; all listed heap/live values therefore remain historical.
- CF-24 `[SOURCE]` confirmed server-side NPC presentation path is MySQL `npc(npcid,name,gfxid)` → `NpcTable/L1Npc` → `L1NpcInstance` → `S_NPCPack` → client sprite resolver → SPR rendering.
- CF-25 `[SOURCE]` server item packets use `item.get_gfxid()`; item DB records use `invgfx` for inventory icon selection. The exact 8.8 client container/item-common.bin resolution remains unverified.
- CF-26 `[TOOL+VISUAL]` GFX 13715 resource test found no Sprite10 `13715-0`; actual file was `13715-18.spr`, 12 frames, with frame 3 recognizable as character-plus-bow. Artifact: `test_13715_Sprite10.png` in the prior local tool output context.
- CF-27 `[TOOL]` four resource CLI actions took about 43 seconds, making export-all unsuitable for interactive lookup; index-first/on-demand extraction is the preferred design.
- CF-28 `[TOOL/config]` Argus MCP elevation was configured through `C:\Program Files\gsudo\2.6.1\gsudo.exe --direct` launching the original Argus virtual-environment Python module while preserving `PYTHONPATH` and `ARGUS_RETK_ROOT`; Tripwire config was not modified.
- CF-29 `[WORKFLOW]` durable workflow files are `AI_HANDOFF.md`, `CODEX_STATUS.md`, `CLIENT_UI_FUNCTION_MAP.md`, and `REVIEWER_FEEDBACK.md`; Executor updates status every Goal and pushes evidence checkpoints, while latest explicit user Goal overrides stale reviewer text.
- CF-30 `[POLICY+LEDGER]` J.J. research baseline is `88/783`, remaining `695`; rereads add zero; future batches require a new user Goal, contain at most 10 articles, preserve full article-level technical content, update ledger/tracker/progress, commit+push, then stop.

## 4. Client

- Main durable map: `CLIENT_UI_FUNCTION_MAP.md` sections for RenewalInventoryUI, source/rebuild, order mechanisms, named sort family, AOBs, and negative evidence.
- Runtime values in CF-04/CF-05 are anchors for interpreting old captures only. New session sequence is process attach → module identity → AOB/byte rebind → singleton/object validation → read-only snapshots.
- Smallest unresolved capture is not another data watch. It is one exact execution capture across `DFB6E0`, `DFC6B0`, `DE0E40`, `DFA580` during one user-visible drag that actually changes order.

## 5. Server

- Server source is in `src/com/lineage/server`; database/update material is under `資料庫&修正歷程_工具`.
- Only CF-24/CF-25 are promoted here as confirmed cross-layer presentation facts. No server feature code was changed in this archival turn.
- The primary user worktree contains unrelated uncommitted server edits. Preserve them; do not reset, checkout-overwrite, or include them in an archive commit.

## 6. Protocol

- Confirmed scope is limited to server packet construction passing GFX/item presentation values into the client-facing path.
- No opcode, byte layout, packet mutation, or server-state reorder protocol has been proven by this chat. Any such claim remains `待 Runtime / Packet 驗證`.

## 7. Resources / Tools

- Resource principle: retain potentially useful old-version knowledge but label compatibility. Do not infer 8.8 loader behavior from tool UI or filename alone.
- Preferred performance path: enumerate/index first, extract a requested GFX/SPR only, cache metadata, render only selected frames.
- Argus boundary: read-only attach, memory reads, disassembly, and narrowly scoped breakpoints are allowed when requested. No patch/write/direct client calls/Tripwire without a new explicit Goal.

## 8. Runtime / Validation Evidence

| ID | Purpose | Method | Result | Status | Durable source |
|---|---|---|---|---|---|
| VT-01 | Identify new-item grid writer | single `find_what_writes` on grid vector end during purchase | `FBC6F0 → FBC7CC → FC4A70`, writer `FC4BCE` | SUCCESS, single append only | CF-12/13; Function Map |
| VT-02 | Recover bulk grid population | static callers plus live inventory/UI behavior | main refresh chain mapped to `DFA580/FC2520`; BA chain later classified PromoteDoll | SUCCESS with role correction | Function Map |
| VT-03 | Map PromoteDollUI | RTTI/vtable/global/source-container analysis | distinct UI, shared grid ABI, source order preserved | SUCCESS | Function Map |
| VT-04 | Test DE1D40 table at initialization | execution capture/read 180 DWORDs | table all zero at initialization | NEGATIVE for live reorder proof | Function Map/status history |
| VT-05 | Test live order table after manual move | read/watch table and compare | no evidence that the observed move populated/used it | NEGATIVE | Function Map/status history |
| VT-06 | Test icon `+0x94` as reorder field | two short data watches | many idle POINT writes; writer `4C32B8`, not reorder | DISPROVEN | Function Map |
| VT-07 | Compare four order representations after drag attempt | before/after icon/source/layout/persisted snapshots | no relevant change; event did not prove reorder | INCONCLUSIVE event | Function Map |
| VT-08 | Validate capture harness offline | `inventory_reorder_capture.py selftest` | PASS, five anchors, zero failures | SUCCESS | local-only tool; hashes below |
| VT-09 | Validate GFX 13715 asset | targeted lookup/extract/render | `13715-18.spr`, 12 frames; frame 3 visually identified | SUCCESS for this asset | prior resource test output |

## 9. Research Evidence

Every entry below is preserved as potentially useful external/older-version evidence. None is an 8.8 fact without a separate local proof. Full per-article provenance and URLs live in the JJ files.

- RE-01 `[research evidence][版本未確認][8.8 未驗證]` 3.81C/L1J articles describe client/server/resource customization patterns; terminology may transfer, offsets and file paths may not.
- RE-02 `[research evidence][待驗證]` morph-related `list.spz`, `list.spr`, and custom PAK workflows may control sprite lookup/morph resources.
- RE-03 `[research evidence][待驗證]` custom PAK/container precedence and mutation behavior may depend on client build and launcher/update logic.
- RE-04 `[research evidence][版本未確認]` reported GFX remapping example such as `#5641 64=240` is a lead, not a confirmed 8.8 mapping.
- RE-05 `[research evidence][待驗證]` `spr_action` metadata may influence animation timing/action dispatch.
- RE-06 `[research evidence][待驗證]` sprites may be composed from multiple visual/equipment layers rather than one standalone SPR.
- RE-07 `[research evidence][8.8 未驗證]` historical item-icon maps connect DB `invgfx` to client resource records; exact current container/record format is unknown.
- RE-08 `[research evidence][版本未確認]` legacy resource-tool capability matrices mention IDX/PAK/SPR/BIN operations; similar tool names do not prove equivalent functions.
- RE-09 `[research evidence][待驗證]` XML/SPZ/HTML encryption/decryption workflows require source provenance, round-trip, and actual 8.8 loader acceptance tests.
- RE-10 `[research evidence][版本未確認]` historical `storeItem` stack/new-item behavior is relevant to server inventory logic but cannot be applied to this source/build without code verification.
- RE-11 `[research evidence][版本未確認]` older launcher/patcher component roles may explain resource precedence but require matching binaries/configs.
- RE-12 `[research evidence][待驗證]` network/map layering articles may expose protocol or map-format leads; exact 8.8 opcodes/layouts are not established.

Research checkpoint state:

- Latest detailed reread: `JJ_RESEARCH_PROGRESS_20260907_1816.md`; nine PUBLIC_FULL articles were already present in earlier ledgers, so completed count stayed 88.
- Canonical tracking: `JJ_SITE_RESEARCH_TRACKER.md`, `JJ_LINEAGE_RESEARCH_MAP.md`, addendum, and timestamped progress files.
- Content rule: preserve Client/Server/Protocol/resource tools/modification tools/packets/file formats even when uncertain; remove only true duplicates.

## 10. Failed / Rejected Directions

- FR-01 Never reuse old PID, heap object, singleton live value, vector address, or data-watch address after restart.
- FR-02 Do not run three parallel `find_what_writes`; it previously occupied the Argus worker. Use one minimal watch only when a data writer is still the actual question.
- FR-03 Do not wait 5–10 minutes for a watch after the requested event; read current hit state immediately and stop/clear stale work.
- FR-04 Do not repeat DE1D40 initialization capture; all-zero initialization did not identify the live order producer.
- FR-05 Do not treat manager `+0x68` table as live manual order merely because DE1D40 references it statically.
- FR-06 Do not watch icon `+0x94/+0x98` again for reorder; it is an idle POINT/layout field with generic layout writer.
- FR-07 Do not treat PromoteDollUI `BA5800/BA5820/BA1AF0` as main inventory rebuild.
- FR-08 Do not infer the named sort engine is on the main inventory path; exact link remains absent.
- FR-09 Do not directly call `FBC6F0` as a PoC; it creates/appends one icon and is not the batch reorder contract.
- FR-10 Do not infer a drag happened from mouse action alone; success requires changed representation(s) and correlated execute hits.
- FR-11 Do not use process-wide/global scans for the next inventory step; bound work to four rebased anchors and four known vectors.
- FR-12 Do not export all resources interactively; measured CLI startup/action cost makes that path too slow.
- FR-13 Do not patch, write client memory, inject DLLs, call client functions, or use Tripwire without an explicit separate authorization.
- FR-14 Do not automatically continue J.J. research or process the remaining corpus; wait for a new user Goal and cap the batch at 10.

## 11. Important Files and Paths

Repository documents:

| Path | Use / importance |
|---|---|
| `docs/AI_HANDOFF.md` | invariant execution, evidence, Git, and handoff rules |
| `docs/CODEX_STATUS.md` | only current Goal/state; replace each round rather than append history |
| `docs/CLIENT_UI_FUNCTION_MAP.md` | durable client UI structures, VAs, call chains, AOBs, negative evidence |
| `docs/REVIEWER_FEEDBACK.md` | latest reviewer action when not superseded by a newer user Goal |
| `docs/INVENTORY_REORDER_CAPTURE_PACK.md` | exact four-anchor live capture procedure and signatures |
| `docs/INVENTORY_REORDER_CAPTURE_RESULT.md` | pending result template; currently not a successful live capture |
| `docs/JJ_SITE_RESEARCH_TRACKER.md` | formal research ledger/count and batch policy |
| `docs/JJ_LINEAGE_RESEARCH_MAP.md` | consolidated external research knowledge map |
| `docs/JJ_LINEAGE_RESEARCH_MAP_ADDENDUM_20260907_1623.md` | later detailed additions |
| `docs/JJ_RESEARCH_PROGRESS_20260907_1816.md` | latest detailed reread/checkpoint |
| `docs/CHAT_HANDOFF_20260907.md` | older snapshot; useful provenance but superseded where this archive differs |

Local-only/untracked tooling; do not assume another host has it:

| Path | Purpose | SHA-256 / state |
|---|---|---|
| `C:\架設功具\dc  880伺服器\tools\inventory_reorder_capture.py` | offline/probe/capture/selftest harness | `6E414BFD7477C10D7E841B5B66D43BEB9908BC9E92B15438EBDE94D9164BF102`; 54,925 bytes |
| `C:\架設功具\dc  880伺服器\tools\inventory_reorder_anchors.json` | anchors/AOBs/expected roles | `DAF93D33A2D59CE1DE3B556C03E1F6DADF8F36B929803DD10D63E22CC4FD616C`; 10,287 bytes |
| `C:\架設功具\dc  880伺服器\tools\watch_reviewer_feedback.ps1` | remote feedback polling helper | `9731ADA9E424A71BEFCBE54D7484B79218F21E18AC1E6916C0F99717B8809D50`; 4,818 bytes |
| `C:\架設功具\dc  880伺服器\runtime_static_bridge\LinLogin.loaded-image.bin` | local loaded-image/static bridge | 30,875,648 bytes; never commit; manifest SHA-256 `60600d7b244a32bd5f17505bb97524b86be06293eb9f90d6b4ac3df0fcf6ba15` |
| `C:\Users\User\.codex\config.toml` | Argus/Tripwire MCP definitions | Argus gsudo elevation confirmed; preserve env values |
| `C:\Users\User\.codex\automations\reviewer-feedback\automation.toml` | reviewer monitor definition | `PAUSED` at archive time; hourly rule; do not assume active |

Workspace safety:

- Primary worktree `C:\架設功具\dc  880伺服器` contains user changes across `.gitignore`, `ServerStart.bat`, core docs, multiple Java files, SQL files, and one untracked Java table class. Treat all as user-owned.
- Archival/research commits were prepared in independent worktree `C:\Users\User\AppData\Local\Temp\lineage-880-jj-checkpoint` to avoid those changes.

## 12. Current State

- Archive-time client state: not running; no valid live attach/heap pointers.
- Inventory capture package: offline selftest ready; live result remains pending.
- J.J. corpus: 88 complete, 695 remaining; next batch blocked only by explicit user start instruction.
- Reviewer feedback file still describes the one-drag inventory capture and can be stale relative to a newer explicit Goal.
- Reviewer automation exists but is paused. No automatic site-research scheduler should be inferred from older handoff text.
- This archive turn changes docs only. No Java, client, server, database, PAK/IDX/SPR, or runtime state is changed.

## 13. Open Questions

- OQ-01 `[待 Runtime 驗證]` Do all four live anchor AOBs uniquely rebind in the next LinLogin build/session?
- OQ-02 `[待 Runtime 驗證]` What are exact `this`, registers, stack args, and return effects for DFB6E0/DFC6B0/DE0E40/DFA580?
- OQ-03 `[待 Runtime 驗證]` Which anchors fire, and in what order, for one drag that visibly changes inventory ordering?
- OQ-04 `[待 Runtime 驗證]` Which of manager+0x54, UI+0x16C, grid+0x1BC, and grid+0x190 actually change?
- OQ-05 `[待 Protocol/Server 驗證]` Is reorder purely local UI state, persisted client-side, or communicated to server?
- OQ-06 `[待 Client 驗證]` Who produces the manager+0x54 order consumed by the main rebuild path?
- OQ-07 `[待 Client 驗證]` Who writes/uses DE1D40 manager+0x68/+0x340 order tables after fully loaded gameplay?
- OQ-08 `[待 Client 驗證]` Is the named ItemSort/merge-sort family used by another inventory command or only unrelated systems?
- OQ-09 `[待 Resource 驗證]` What exact 8.8 record/container resolves DB `invgfx` to an icon asset?
- OQ-10 `[待 Resource 驗證]` What are the true 8.8 morph-list source and custom-resource precedence rules?
- OQ-11 `[待 Resource 驗證]` Which XML/SPZ/HTML encryption variants round-trip and are accepted by the 8.8 loader?
- OQ-12 `[待 Client/Resource 驗證]` How does the client choose numeric SPR suffixes such as `13715-18`?
- OQ-13 `[待人工決策]` Should the local capture harness/anchor JSON be reviewed and added to Git, or remain local-only?
- OQ-14 `[待人工決策]` Should the paused reviewer-feedback automation be resumed or replaced?
- OQ-15 `[待 Git 整理]` How should primary-worktree user edits be reconciled without mixing them into documentation checkpoints?
- OQ-16 `[待 Research ledger 驗證]` Complete the exact 783-title/access/category ledger and verify category totals without inflating the 88 baseline.

## 14. Next Actions

- NA-01 On a new chat/task, read this archive plus the four workflow docs before acting; do not reconstruct state from memory.
- NA-02 If the Goal is inventory runtime work, run local harness selftest/offline validation and inspect current process state before asking for gameplay input.
- NA-03 With LinLogin running, attach read-only, AOB-rebind four anchors, validate singleton/containers, record four before snapshots, set four execute breakpoints, then report `CAPTURE_READY`.
- NA-04 Request exactly one user drag that visibly reorders an item; capture hits/ABI/return state, take after snapshots, clear breakpoints/detach, update result/status/map, commit+push.
- NA-05 Only if NA-04 proves the contract, specify a minimal PoC call sequence; implementation involving client calls/write/patch/injection requires a separate explicit authorization.
- NA-06 If the Goal is J.J. research, process no more than 10 articles with full per-article content, de-duplicate against ledger, update tracker/map/progress, commit+push, and stop.
- NA-07 If the Goal is resource lookup, use index-first/on-demand extraction and validate the requested GFX/frame rather than export-all.
- NA-08 Before any Git write, fetch/status; because the primary worktree is dirty, use a safe independent worktree and never force-push.

## 15. Existing GitHub Documents to Read

Read order for a replacement AI:

1. [`CHAT_ARCHIVE_20260907_1844.md`](CHAT_ARCHIVE_20260907_1844.md) — compact context/index.
2. [`AI_HANDOFF.md`](AI_HANDOFF.md) — invariant workflow and evidence rules.
3. [`CODEX_STATUS.md`](CODEX_STATUS.md) — newest Goal state.
4. [`REVIEWER_FEEDBACK.md`](REVIEWER_FEEDBACK.md) — reviewer action unless superseded.
5. [`CLIENT_UI_FUNCTION_MAP.md`](CLIENT_UI_FUNCTION_MAP.md) — exact client facts/AOBs.
6. [`INVENTORY_REORDER_CAPTURE_PACK.md`](INVENTORY_REORDER_CAPTURE_PACK.md) and [`INVENTORY_REORDER_CAPTURE_RESULT.md`](INVENTORY_REORDER_CAPTURE_RESULT.md) — live capture procedure/state.
7. [`JJ_SITE_RESEARCH_TRACKER.md`](JJ_SITE_RESEARCH_TRACKER.md), [`JJ_LINEAGE_RESEARCH_MAP.md`](JJ_LINEAGE_RESEARCH_MAP.md), [`JJ_LINEAGE_RESEARCH_MAP_ADDENDUM_20260907_1623.md`](JJ_LINEAGE_RESEARCH_MAP_ADDENDUM_20260907_1623.md), [`JJ_RESEARCH_PROGRESS_20260907_1816.md`](JJ_RESEARCH_PROGRESS_20260907_1816.md) — research ledger and evidence.

End condition: preserve this archive in `origin/main`; deletion of the chat remains a user-only action.
